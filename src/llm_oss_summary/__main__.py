"""Entry point: `uv run python -m llm_oss_summary`.

Currently a placeholder — wire up CLI subcommands here as the pipeline grows.
"""


def main() -> None:
    print("llm-oss-summary: pipeline not yet wired up. See docs/roadmap.md for next steps.")


if __name__ == "__main__":
    main()
